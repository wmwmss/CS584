The code can be run in any Python interpreter. However, the paths to load the data files need to be specified. 

After the code runs, the results are generated and put in submission.txt

The final results in submission.txt may need to have its header and spaces removed to be compatible to the miner2 submission. 
